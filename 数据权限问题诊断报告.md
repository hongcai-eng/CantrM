# 数据权限问题诊断报告

## 🔍 问题根源（已发现）

从诊断输出可以看到：

### 用户1：张三
- **User.organization_id**: 1（管理部）✅
- **岗位**: 测试岗位-全部合同
- **问题**: `UserPosition.organization_id: None` ❌
- **结果**: 虽然岗位数据权限是"全部合同"，但因为没有组织关联，可能影响其他功能

### 用户2：李四
- **User.organization_id**: 2（技术部）✅
- **岗位**: 测试岗位-本组织
- **岗位数据权限**: org（本组织合同）
- **关键问题**: `UserPosition.organization_id: None` ❌❌❌
- **UserOrganization 记录**: 0 条 ❌

**这就是问题所在！**

---

## ❌ 核心问题

**当岗位数据权限为"本组织合同"（org）时：**
1. 系统会查找 `UserOrganization` 表中 `is_primary=True` 的记录
2. 获取用户的主组织ID
3. 返回该组织的所有合同

**但现在的情况是：**
- `UserPosition.organization_id = None`（岗位分配时没有选择组织）
- `UserOrganization` 表中没有记录
- 系统找不到用户的主组织
- **结果**：用户只能看到自己创建和负责的合同，看不到组织的合同

---

## ✅ 解决方案

### 方案1：重新分配岗位（推荐）

**操作步骤**：
1. 进入 **"岗位管理"**
2. 找到该岗位，点击 **"分配用户"**
3. 在"当前岗位人员明细"中，点击每个用户的 **"移除"** 按钮
4. 重新点击 **"添加新用户"**
5. 选择用户
6. **重要**：**必须选择"所属组织"**（如：管理部）
7. **勾选"设为主岗位"**
8. 保存
9. 用户刷新页面即可看到组织合同

### 方案2：使用SQL直接修复（快速）

如果用户很多，可以用SQL批量修复：

```sql
-- 备份现有数据
SELECT * FROM user_position;

-- 更新 UserPosition 表，将用户的主组织同步到岗位
UPDATE user_position 
SET organization_id = (SELECT organization_id FROM user WHERE user.id = user_position.user_id)
WHERE organization_id IS NULL;

-- 为每个用户创建 UserOrganization 记录
INSERT INTO user_organization (user_id, organization_id, is_primary)
SELECT DISTINCT up.user_id, u.organization_id, 1
FROM user_position up
JOIN user u ON u.id = up.user_id
WHERE u.organization_id IS NOT NULL
AND NOT EXISTS (
    SELECT 1 FROM user_organization uo 
    WHERE uo.user_id = up.user_id 
    AND uo.organization_id = u.organization_id
);
```

**执行方式**：
```bash
python fix_user_organizations.py
```

---

## 📋 验证步骤

修复后，运行以下命令验证：

```python
python -c "
from app import app, db, get_user_accessible_contract_ids
from models import User

with app.app_context():
    user = User.query.filter_by(username='李四').first()
    if user:
        contracts = get_user_accessible_contract_ids(user.id, user.customer_id)
        print(f'用户可访问的合同数: {len(contracts) if contracts else \"全部\"}')
"
```

---

## 🎯 预防措施

已在代码中添加验证（app.py 第1406-1409行）：

```python
# 如果岗位的数据权限是"本组织合同"，但未指定组织，给出提示
if position.data_scope == 'org' and not org_id:
    flash(f'岗位"{position.name}"的数据权限为"本组织合同"，必须指定所属组织，否则用户无法访问组织数据', 'warning')
    return redirect(url_for('positions'))
```

**以后分配用户时**：
- 系统会强制要求选择组织
- 防止再次出现此问题

---

## 🔧 快速修复脚本

已创建修复脚本：`fix_user_organizations.py`

**功能**：
1. 自动同步 User.organization_id 到 UserPosition.organization_id
2. 自动创建 UserOrganization 记录
3. 设置主组织标记

**使用方法**：
```bash
python fix_user_organizations.py
```

---

## 📌 总结

**问题**：用户在组织中，岗位有"本组织合同"权限，但看不到合同

**原因**：
1. `UserPosition.organization_id = None`（没有组织关联）
2. `UserOrganization` 表中没有记录（系统找不到主组织）

**解决**：
- 方案1：重新分配岗位，选择组织（推荐）
- 方案2：运行修复脚本（快速）

**预防**：已添加代码验证，以后不会再出现此问题
