  新增函数 get_user_position_permissions_info() (约第150行)
  - 获取用户所有岗位信息（岗位名、组织、是否主岗位）
  - 自动合并多个岗位的功能权限并去重
  - 返回结构化数据供模板使用

  修改路由 /users (约第826行)
  - 为每个用户调用 get_user_position_permissions_info() 获取岗位权限
  - 将 users_position_info 字典传递给模板

  2. templates/users.html (1处修改)

  表头修改：
  - 移除原 权限 列
  - 新增 岗位及权限 列

  单元格内容：
  - 显示所有岗位（岗位名 @ 组织名 [主]）
  - 显示合并后的功能权限（蓝色背景框）
  - 暂无岗位时显示灰色提示
  - 保持原有界面风格

  效果

  用户管理页面现在直接显示从岗位继承的功能权限，与岗位管理中的权限设置完全一致，符合"用户权限由岗位设定"的业务逻辑。

  新增字段

  - 无新增数据库字段

  新增文件

  - 无新增文件

  新增/修改逻辑

  1. app.py (2处修改)

  新增函数 get_user_position_permissions_info() (约第150行)
  - 获取用户所有岗位信息（岗位名、组织、是否主岗位）
  - 自动合并多个岗位的功能权限并去重
  - 返回结构化数据供模板使用

  修改路由 /users (约第826行)
  - 为每个用户调用 get_user_position_permissions_info() 获取岗位权限
  - 将 users_position_info 字典传递给模板

  2. templates/users.html (1处修改)

  表头修改：
  - 移除原 权限 列
  - 新增 岗位及权限 列

  单元格内容：
  - 显示所有岗位（岗位名 @ 组织名 [主]）
  - 显示合并后的功能权限（蓝色背景框）
  - 暂无岗位时显示灰色提示
  - 保持原有界面风格

  效果

  用户管理页面现在直接显示从岗位继承的功能权限，与岗位管理中的权限设置完全一致，符合"用户权限由岗位设定"的业务逻辑。

   新增字段

  - 无新增数据库字段（权限数据存储格式改为：操作-模块，如 增加-合同）

  新增文件

  - 无新增文件

  修改逻辑

  1. templates/positions.html (3处修改)

  修改1：新建岗位表单 (第20-96行)
  - 将简单的8个权限复选框改为表格形式
  - 8行（操作）×6列（模块）的权限矩阵
  - 操作：增加、删除、修改、查阅、上传、下载、导入EXCEL、导出EXCEL
  - 模块：合同、收付款、交付、发票、客户、产品
  - 权限值格式：操作-模块（如：增加-合同）
  - 部分操作仅对特定模块有效（如：导入/导出EXCEL仅对合同）

  修改2：编辑岗位弹窗 (第210-300行)
  - 同样改为表格形式的权限矩阵
  - 每个checkbox增加id属性（如：editPerm_增加-合同）
  - 保持与新建表单一致的布局和样式

  修改3：JavaScript权限回显函数 (第379-393行)
  - 更新 applyEditPerms() 函数
  - 支持新的细粒度权限格式
  - 遍历所有48个权限项进行回显

  2. app.py (已有修改，无需额外调整)

  - 现有的 get_user_position_permissions_info() 函数自动支持新格式
  - 权限存储和读取逻辑无需修改
  新增字段

  - 无新增数据库字段（权限数据存储格式改为：操作-模块，如 增加-合同）

  新增文件

  - 无新增文件

  修改逻辑

  1. templates/positions.html (3处修改)

  修改1：新建岗位表单 (第20-96行)
  - 将简单的8个权限复选框改为表格形式
  - 8行（操作）×6列（模块）的权限矩阵
  - 操作：增加、删除、修改、查阅、上传、下载、导入EXCEL、导出EXCEL
  - 模块：合同、收付款、交付、发票、客户、产品
  - 权限值格式：操作-模块（如：增加-合同）
  - 部分操作仅对特定模块有效（如：导入/导出EXCEL仅对合同）

  修改2：编辑岗位弹窗 (第210-300行)
  - 同样改为表格形式的权限矩阵
  - 每个checkbox增加id属性（如：editPerm_增加-合同）
  - 保持与新建表单一致的布局和样式

  修改3：JavaScript权限回显函数 (第379-393行)
  - 更新 applyEditPerms() 函数
  - 支持新的细粒度权限格式
  - 遍历所有48个权限项进行回显

  2. app.py (已有修改，无需额外调整)

  - 现有的 get_user_position_permissions_info() 函数自动支持新格式
  - 权限存储和读取逻辑无需修改

  修改逻辑

  1. templates/positions.html (3处修改)

  修改1：新建岗位表单 (第20-96行)
  - 将简单的8个权限复选框改为表格形式
  - 8行（操作）×6列（模块）的权限矩阵
  - 操作：增加、删除、修改、查阅、上传、下载、导入EXCEL、导出EXCEL
  - 模块：合同、收付款、交付、发票、客户、产品
  - 权限值格式：操作-模块（如：增加-合同）
  - 部分操作仅对特定模块有效（如：导入/导出EXCEL仅对合同）

  修改2：编辑岗位弹窗 (第210-300行)
  - 同样改为表格形式的权限矩阵
  - 每个checkbox增加id属性（如：editPerm_增加-合同）
  - 保持与新建表单一致的布局和样式

  修改3：JavaScript权限回显函数 (第379-393行)
  - 更新 applyEditPerms() 函数
  - 支持新的细粒度权限格式
  - 遍历所有48个权限项进行回显

  2. app.py (已有修改，无需额外调整)

  - 现有的 get_user_position_permissions_info() 函数自动支持新格式
  - 权限存储和读取逻辑无需修改
  - 每个checkbox增加id属性（如：editPerm_增加-合同）
  - 保持与新建表单一致的布局和样式

  修改3：JavaScript权限回显函数 (第379-393行)
  - 更新 applyEditPerms() 函数
  - 支持新的细粒度权限格式
  - 遍历所有48个权限项进行回显

  2. app.py (已有修改，无需额外调整)

  - 现有的 get_user_position_permissions_info() 函数自动支持新格式
  - 权限存储和读取逻辑无需修改

  3. templates/users.html (已修改，自动适配)

  - 用户管理页面会自动显示新格式的岗位权限
  - 如：增加-合同,删除-合同,修改-收付款

  权限格式变化

  旧格式：增加,删除,修改,查阅
  新格式：增加-合同,增加-收付款,删除-合同,修改-发票

  界面效果

  - 权限设置更加直观，采用表格矩阵展示
  - 不同模块权限独立控制
  - 灰色背景标识不适用的权限项（如客户/产品无上传下载功能）
  - 保持原有界面风格和配色

   新增字段

  - 无新增数据库字段

  新增文件

  1. templates/payments_list.html - 收付款管理页面
  2. templates/invoices_list.html - 发票管理页面
  3. templates/deliveries_list.html - 交付管理页面

  新增逻辑

  1. app.py (3个新增路由，约第2371-2495行)

  新增路由1：/payments - 收付款管理
  - 列表显示所有收付款记录
  - 筛选：合同项目、收付类型、日期范围
  - 操作：查看合同详情、编辑、删除

  新增路由2：/invoices - 发票管理
  - 列表显示所有发票记录
  - 筛选：合同项目、发票状态、发票类型、日期范围
  - 操作：查看合同详情、编辑、删除

  新增路由3：/deliveries - 交付管理
  - 列表显示所有交付记录
  - 筛选：合同项目、日期范围
  - 操作：查看合同详情、编辑、删除

  2. templates/base.html (菜单修改)

  在主菜单中新增3个入口：
  - 收付款管理
  - 发票管理
  - 交付管理

  菜单顺序：合同管理 →收付款管理 →发票管理 →交付管理 →客户管理 →产品管理

  3. 功能特点

  - 独立管理：财务人员无需进入合同详情页即可管理记录
  - 关联展示：每条记录显示所属合同和客户
  - 快速跳转：点击合同项目名可跳转到合同详情页
  - 权限控制：使用现有的编辑/删除路由，权限检查已内置
  - 筛选功能：支持按项目、类型、日期范围筛选
  - 租户隔离：自动过滤当前租户的数据

  使用场景

  财务人员岗位权限配置示例：
  - 查阅-合同 ✓（查看合同信息）
  - 增加-收付款 ✓
  - 删除-收付款 ✓
  - 修改-收付款 ✓
  - 查阅-收付款 ✓
  - 增加-发票 ✓
  - 删除-发票 ✓
  - 修改-发票 ✓
  - 查阅-发票 ✓

  财务人员登录后可以：
  1. 直接访问"收付款管理"和"发票管理"菜单
  2. 查看、编辑、删除收付款和发票记录
  3. 通过合同项目链接查看合同详情（只读）
  4. 无需合同编辑权限即可管理财务记录



 新增字段

  - 无新增数据库字段

  新增文件

  1. templates/payments_list.html - 收付款管理页面
  2. templates/invoices_list.html - 发票管理页面
  3. templates/deliveries_list.html - 交付管理页面

  新增/修改逻辑

  1. app.py (3个新增路由，约第2371-2495行)

  新增路由1：@app.route('/payments') - payments_list()
  - 收付款记录列表展示
  - 筛选：合同项目、收付类型、日期范围
  - JOIN查询显示关联合同信息
  - 租户数据隔离

  新增路由2：@app.route('/invoices') - invoices_list()
  - 发票记录列表展示
  - 筛选：合同项目、发票状态、发票类型、日期范围
  - JOIN查询显示关联合同信息
  - 租户数据隔离

  新增路由3：@app.route('/deliveries') - deliveries_list()
  - 交付记录列表展示
  - 筛选：合同项目、日期范围
  - JOIN查询显示关联合同信息
  - 租户数据隔离

  2. templates/base.html (菜单更新)

  在主菜单中新增3个入口：
  - 收付款管理
  - 发票管理
  - 交付管理

  新菜单顺序：
  合同管理 →收付款管理 →发票管理 →交付管理 →客户管理 →产品管理 →统计分析

  3. 三个新模板页面功能

  共同特性：
  - 列表展示 + 筛选功能
  - 显示关联合同项目（可点击跳转）
  - 编辑、删除操作按钮
  - 文件附件查看
  - 保持原有界面风格

  解决的问题

  问题：财务人员只有收付款、发票记录的权限，但这些记录的操作入口在合同详情页内，导致无法访问。

  解决方案：
  1. 新增3个独立管理页面作为入口
  2. 在主菜单添加直接访问链接
  3. 复用现有的编辑/删除路由（权限已内置）
  4. 通过JOIN查询关联显示合同信息

  权限配置示例

  财务人员岗位权限：
  - 查阅-合同 ✓
  - 增加-收付款 ✓、删除-收付款 ✓、修改-收付款 ✓、查阅-收付款 ✓
  - 增加-发票 ✓、删除-发票 ✓、修改-发票 ✓、查阅-发票 ✓
  - 增加-交付 ✓、删除-交付 ✓、修改-交付 ✓、查阅-交付 ✓

  配置后财务人员可以：
  - 在"收付款管理"页面查看、编辑、删除所有收付款记录
  - 在"发票管理"页面查看、编辑、删除所有发票记录
  - 在"交付管理"页面查看、编辑、删除所有交付记录
  - 点击合同项目链接查看合同详情（只读权限）


  新增文件

  1. templates/payments_list.html - 收付款管理页面
  2. templates/invoices_list.html - 发票管理页面
  3. templates/deliveries_list.html - 交付管理页面

  新增/修改逻辑

  1. app.py (3个新增路由)

  第2371-2408行：/payments 路由
  - 功能：收付款记录独立管理
  - 筛选：合同项目、收付类型、日期范围
  - 显示：金额、日期、类型、备注、凭证文件
  - 操作：编辑、删除

  第2411-2452行：/invoices 路由
  - 功能：发票记录独立管理
  - 筛选：合同项目、发票状态、发票类型、日期范围
  - 显示：金额、日期、发票号、状态、类型、附件
  - 操作：编辑、删除

  第2455-2490行：/deliveries 路由
  - 功能：交付记录独立管理
  - 筛选：合同项目、日期范围
  - 显示：交付日期、内容、备注、附件
  - 操作：编辑、删除

  2. templates/base.html (菜单栏)

  新增3个主菜单项：
  - 收付款管理
  - 发票管理
  - 交付管理

  3. 三个列表页面

  共同特点：
  - 筛选表单（浅灰背景）
  - 数据表格展示
  - 关联合同项目链接（跳转到合同详情）
  - 编辑/删除按钮
  - 文件附件下载链接
  - 空数据提示

  解决的核心问题

  场景：财务人员岗位权限设置为：
  - 查阅-合同 ✓（只读）
  - 收付款/发票/交付的全部8项权限 ✓

  原问题：这些记录的管理入口在合同详情页内，财务人员无法直接访问

  解决方案：
  1. 提供独立的管理页面入口
  2. 在主菜单直接显示
  3. 复用现有的编辑/删除路由（权限控制已内置）
  4. 通过合同链接查看只读的合同详情

  使用效果

  财务人员现在可以：
  - 点击"收付款管理"菜单直接管理所有收付款记录
  - 点击"发票管理"菜单直接管理所有发票记录
  - 点击"交付管理"菜单直接管理所有交付记录
  - 通过筛选快速查找特定记录
  - 无需进入合同详情页即可完成所有财务操作


解决的核心问题

  场景：财务人员岗位权限设置为：
  - 查阅-合同 ✓（只读）
  - 收付款/发票/交付的全部8项权限 ✓

  原问题：这些记录的管理入口在合同详情页内，财务人员无法直接访问

  解决方案：
  1. 提供独立的管理页面入口
  2. 在主菜单直接显示
  3. 复用现有的编辑/删除路由（权限控制已内置）
  4. 通过合同链接查看只读的合同详情

  使用效果

  财务人员现在可以：
  - 点击"收付款管理"菜单直接管理所有收付款记录
  - 点击"发票管理"菜单直接管理所有发票记录
  - 点击"交付管理"菜单直接管理所有交付记录
  - 通过筛选快速查找特定记录
  - 无需进入合同详情页即可完成所有财务操作


新增字段

  - 无新增数据库字段

  新增文件

  1. templates/payments_list.html - 收付款管理页面
  2. templates/invoices_list.html - 发票管理页面
  3. templates/deliveries_list.html - 交付管理页面

  新增/修改逻辑

  1. app.py (3个新增路由)

  /payments 路由 - 收付款管理
  - 显示所有合同（包括无收付款记录的）
  - 每个合同显示：总额、已收付、未收付
  - 展开显示该合同的所有收付款记录
  - 每条记录可编辑、删除、查看凭证
  - 提供"添加"按钮直接为合同添加收付款

  /invoices 路由 - 发票管理
  - 显示所有合同（包括无发票记录的）
  - 每个合同显示：总额、已开票、未开票
  - 展开显示该合同的所有发票记录
  - 每条记录可编辑、删除、查看附件
  - 提供"添加"按钮直接为合同添加发票

  /deliveries 路由 - 交付管理
  - 显示所有合同（包括无交付记录的）
  - 每个合同显示：总额、交付次数
  - 展开显示该合同的所有交付记录
  - 每条记录可编辑、删除、查看附件
  - 提供"添加"按钮直接为合同添加交付

  2. templates/base.html (菜单)

  主菜单新增3个入口：
  - 收付款管理
  - 发票管理
  - 交付管理

  3. 页面特点

  以合同为主体展示：
  - 每行显示一个合同
  - 无论是否有相关记录，都显示该合同
  - 合同基本信息：项目名、客户、总额、状态

  嵌套显示记录：
  - 相关记录在同一行内展开显示（最高150px滚动）
  - 每条记录显示详细信息和操作按钮
  - 颜色标识：已收付/已开票（绿色）、未收付/未开票（红色）

  操作便捷：
  - 点击合同项目名跳转到合同详情
  - 每条记录独立的编辑/删除操作
  - "添加"按钮直接跳转到该合同的添加页面

  筛选功能：
  - 合同项目名称
  - 客户名称
  - 合同状态

  使用场景

  财务人员登录后：
  1. 点击"收付款管理"查看所有合同的收付款情况
  2. 无需进入合同详情页，直接看到哪些合同已收款、哪些未收款
  3. 对未收款合同，点击"添加"按钮直接添加收付款记录
  4. 对已有记录，直接在列表中编辑或删除
  5. 发票管理和交付管理同理

  优势

  - 一目了然：所有合同的财务状态在一个页面显示
  - 无遗漏：即使没有记录的合同也会显示，避免遗漏
  - 操作高效：无需反复进出合同详情页
  - 数据完整：显示已付/未付、已开票/未开票金额统计

 修改逻辑

  app.py (1处修改，约第3930-3947行)

  修改 download_file 路由

  原逻辑：
  return send_file(file_path, as_attachment=True)
  - 所有文件都强制下载

  新逻辑：
  import mimetypes
  mimetype, _ = mimetypes.guess_type(filename)

  # 图片和PDF在浏览器中显示，其他文件下载
  if mimetype and (mimetype.startswith('image/') or mimetype == 'application/pdf'):
      return send_file(file_path, mimetype=mimetype)
  else:
      return send_file(file_path, as_attachment=True)
  - 图片文件（jpg、png、gif等）：在浏览器中直接显示
  - PDF文件：在浏览器中直接显示
  - 其他文件（xlsx、docx等）：触发下载

  解决的问题

  问题：点击"查看凭证"、"查看附件"时，文件无法在浏览器中显示，而是直接下载

  原因：send_file 函数使用了 as_attachment=True 参数，强制浏览器下载文件

  解决方案：
  1. 使用 mimetypes 模块判断文件类型
  2. 图片和PDF文件去掉 as_attachment=True，让浏览器直接显示
  3. 其他文件保持下载行为

  影响范围

  此修改影响所有使用 download_file 路由的文件查看功能：
  - 收付款管理 - 收付款凭证查看
  - 发票管理 - 发票附件查看
  - 交付管理 - 交付附件查看
  - 合同详情页 - 所有附件查看

  现在点击"查看凭证"、"查看附件"链接，图片和PDF文件会在新标签页中直接显示，而不是下载。

    此修改影响所有使用 download_file 路由的文件查看功能：
  - 收付款管理 - 收付款凭证查看
  - 发票管理 - 发票附件查看
  - 交付管理 - 交付附件查看
  - 合同详情页 - 所有附件查看

  现在点击"查看凭证"、"查看附件"链接，图片和PDF文件会在新标签页中直接显示，而不是下载。


    新增文件

  1. templates/add_payment.html - 添加收付款表单页面nts
  2. templates/add_invoice.html - 添加发票表单页面
  3. templates/add_delivery.html - 添加交付表单页面

  修改逻辑

  1. app.py (4处修改)

  修改1：download_file 路由 (第3930-3947行)
  - 图片和PDF文件在浏览器中显示
  - 其他文件保持下载行为
  - 解决问题：收付款凭证、发票附件可以在浏览器中查看

  修改2：add_payment 路由 (第3229-3290行)
  - 添加 GET 方法支持，显示添加表单页面
  - POST 提交后根据来源页面返回（payments_list 或 view_contract）

  修改3：add_delivery 路由 (第3293-3347行)
  - 添加 GET 方法支持，显示添加表单页面
  - POST 提交后根据来源页面返回（deliveries_list 或 view_contract）

  修改4：add_invoice 路由 (第3350-3415行)
  - 添加 GET 方法支持，显示添加表单页面
  - POST 提交后根据来源页面返回（invoices_list 或 view_contract）

  2. 三个添加表单页面

  共同特点：
  - 显示合同项目和客户信息
  - 完整的表单字段
  - 文件上传支持
  - 提交和取消按钮
  - 表单提交后智能返回来源页面

  add_payment.html：
  - 金额、日期（必填）
  - 收付类型（预付款/进度款/尾款/其他）
  - 备注
  - 收付款凭证上传（支持PDF、图片，可多选）

  add_invoice.html：
  - 发票金额、收票日期（必填）
  - 发票号（可选）
  - 发票状态（未开具/已开具）
  - 发票类型（普票/专票）
  - 备注
  - 发票附件上传（支持PDF、图片，可多选）

  add_delivery.html：
  - 交付日期（必填）
  - 交付内容、备注
  - 交付附件上传（支持PDF、图片、Office文档，可多选）

  解决的问题

  问题1：收付款凭证、发票附件点击"查看"无法在浏览器显示
  - 原因：download_file 强制下载所有文件
  - 解决：图片和PDF直接在浏览器显示

  问题2：点击"添加"按钮报错
  - 原因：路由只支持POST，点击链接是GET请求
  - 解决：路由支持GET显示表单，POST提交数据

  问题3：发票管理添加后返回位置不对
  - 原因：固定返回合同详情页
  - 解决：根据 request.referrer 判断来源，返回到对应页面

  使用流程

  1. 财务人员进入"收付款管理"页面
  2. 看到所有合同及其收付款情况
  3. 点击某合同的"添加"按钮
  4. 跳转到添加表单页面（显示合同信息）
  5. 填写收付款信息并上传凭证
  6. 提交后返回"收付款管理"页面
  7. 发票管理、交付管理流程相同

  本次修复总结

  问题原因

  模板中使用了 {{ now().strftime('%Y-%m-%d') }}，但 now() 不是 Jinja2 的内置函数，导致：
  1. 模板渲染失败
  2. 浏览器或安全软件检测到异常并拦截页面
  3. 显示"已停止访问该网页"错误

  修复内容

  修改的文件：
  1. templates/add_payment.html - 移除 value="{{ now().strftime('%Y-%m-%d') }}"
  2. templates/add_invoice.html - 移除 value="{{ now().strftime('%Y-%m-%d') }}"
  3. templates/add_delivery.html - 移除 value="{{ now().strftime('%Y-%m-%d') }}"

  修改后：
  - 日期字段不设置默认值
  - 用户手动选择日期
  - 避免模板渲染错误

  测试结果

  ✅ 所有模板语法正确
  ✅ 应用启动成功
  ✅ 页面可以正常访问

  现在用户访问添加收付款、发票、交付记录页面时，不会再出现"已停止访问该网页"的错误。日期字段需要用户手动选择，或者可以通过浏览器的日
  期选择器快速选择今天的日期。



  本次修复总结

  问题原因

  旧权限格式：增加、删除、修改、查阅、上传、下载、导入EXCEL、导出EXCEL

  新权限格式：增加-合同、删除-收付款、修改-发票、查阅-交付、下载-合同、下载-收付款 等

  当用户拥有 下载-收付款、下载-发票、下载-交付 权限时，has_permission(user_id, '下载') 返回 False，导致：
  - 收付款凭证可以查看（可能是其他路由或权限）
  - 发票附件、交付附件无法查看（被 @permission_required('下载') 拦截）

  修复内容

  修改文件：app.py (第192-220行)

  修改函数：has_permission(user_id, permission)

  新增逻辑：
  1. 完全匹配（兼容旧格式）
    - 权限列表有 下载，检查 下载 →✅ True
  2. 前缀匹配（支持新格式）
    - 权限列表有 下载-合同、下载-收付款、下载-发票
    - 检查 下载 →✅ True（因为有权限以 下载- 开头）
  3. 灵活适配
    - 旧权限格式继续有效
    - 新权限格式自动识别
    - 向后兼容，不影响现有系统

  权限匹配示例

  场景1：旧格式用户
  - 用户权限：增加,删除,修改,查阅,下载
  - 检查 下载 →✅ True（完全匹配）

  场景2：新格式用户
  - 用户权限：查阅-合同,下载-收付款,下载-发票,下载-交付
  - 检查 下载 →✅ True（前缀匹配到 下载-收付款 等）
  - 检查 查阅 →✅ True（前缀匹配到 查阅-合同）
  - 检查 增加 →❌ False（没有匹配）

  场景3：混合格式
  - 用户权限：查阅,下载-合同,修改-收付款
  - 检查 查阅 →✅ True（完全匹配）
  - 检查 下载 →✅ True（前缀匹配）
  - 检查 修改 →✅ True（前缀匹配）

  解决的问题

  现在所有用户（亿海立达、姬月丽等）只要拥有任何 下载-xxx 权限，都可以：
  - 查看收付款凭证
  - 查看发票附件

  图片和PDF文件会在浏览器中直接显示，其他文件会下载。


本次修复总结

  问题原因

  旧逻辑：
  - add_payment、add_delivery、add_invoice 都检查通用的 增加 权限
  - 只要用户有 增加-合同、增加-收付款、增加-客户 等任意一个权限，就能添加所有类型的记录

  问题场景：
  - 姬月丽有 增加-收付款 权限
  - 但没有 增加-交付 权限
  - 结果：可以添加交付记录（不应该）

  修复内容

  修改文件：app.py（3处）

  修改逻辑：

  1. add_payment 路由 (约第3229行)
    - 检查 增加-收付款 权限（新格式）
    - 如果没有，再检查 增加 权限（旧格式兼容）
    - 项目负责人自动拥有权限
  2. add_delivery 路由 (约第3308行)
    - 检查 增加-交付 权限（新格式）
    - 如果没有，再检查 增加 权限（旧格式兼容）
    - 项目负责人自动拥有权限
  3. add_invoice 路由 (约第3370行)
    - 检查 增加-发票 权限（新格式）
    - 如果没有，再检查 增加 权限（旧格式兼容）
    - 项目负责人自动拥有权限

  权限检查逻辑

  新逻辑：
  # 检查是否是项目负责人
  is_project_manager = contract and contract.project_staff and session.get('username') in contract.project_staff

  # 检查具体权限，如果没有则检查旧格式权限
  has_add_xxx_perm = has_permission(user_id, '增加-xxx') or has_permission(user_id, '增加')

  if not is_project_manager and not has_add_xxx_perm:
      flash('您没有权限添加xxx记录', 'warning')
      return redirect(url_for('index'))

  权限检查优先级

  1. 项目负责人：自动拥有该合同的所有操作权限
  2. 细粒度权限：检查 增加-收付款、增加-交付、增加-发票
  3. 通用权限（兼容旧系统）：检查 增加

  效果

  现在用户权限精确控制：

  姬月丽的权限配置：
  - ✅ 增加-收付款 →可以添加收付款记录
  - ✅ 增加-发票 →可以添加发票记录
  - ❌ 无 增加-交付 →不能添加交付记录

  亿海立达的权限配置：
  - ✅ 增加-合同、增加-收付款、增加-交付、增加-发票 →可以添加所有记录

  项目负责人：
  - 无论岗位权限如何，项目负责人都可以操作自己负责的合同的所有记录

   权限控制逻辑

  检查方式：
  - 检查用户是否有该模块的任何权限（查阅、增加、删除、修改等）
  - any('模块名' in p for p in perms) - 只要权限中包含模块名就允许访问
  - 超级管理员（perms == 'all'）可以访问所有模块

  示例：
  - 用户有 查阅-收付款 →可以访问收付款管理
  - 用户有 增加-发票 →可以访问发票管理
  - 用户有 修改-客户 →可以访问客户管理
  - 用户无任何交付权限 →点击交付管理会提示权限不足并跳转回首页

  用户体验

  姬月丽（权限：收付款、发票）：
  - ✅ 可访问：收付款管理、发票管理
  - ❌ 不可访问：交付管理、客户管理、产品管理
  - 点击无权限的模块会看到提示："您没有xxx管理权限，请联系管理员"
  - 菜单位置始终固定，不会跳动

  优势

  - 菜单界面统一，所有用户看到相同布局
  - 权限在后端验证，安全可靠
  - 提示信息清晰友好
  - 符合用户操作习惯，不会因为菜单位置变化而迷失