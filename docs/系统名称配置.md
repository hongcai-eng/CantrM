  已将登录界面的"合同管理系统"全部改为"客户管理系统"

  ---
  📋 修改内容

  修改文件

  - templates/login.html (5处修改)

  修改位置

  1. 第6行 - 页面标题：<title>登录 - 客户管理系统</title>
  2. 第28行 - 主标题：<h1>...客户管理系统</h1>
  3. 第97行 - JavaScript默认品牌：'客户管理系统'
  4. 第172行 - JavaScript品牌更新：companyName + '<br>客户管理系统'
  5. 第174行 - JavaScript默认显示：'客户管理系统'

  影响范围

  - 登录页面标题栏显示
  - 登录框主标题
  - 公司品牌动态切换逻辑
  - 无公司时的默认显示

  重启应用后生效。


修改内容：
新增配置字段

  - sys_config.system_name - 系统名称配置（默认值："客户管理系统"）

  修改文件

  1. templates/sysconfig.html
  - 新增"系统名称"输入框（位置：公司名称之前）
  - 显示当前配置值，默认占位符"客户管理系统"

  2. app.py
  - sysconfig() 路由：新增 system_name 到保存列表
  - inject_company() 上下文处理器：读取并注入 system_name 变量
  - login() 路由：读取 system_name 并传递给登录模板

  3. templates/base.html
  - 顶部导航栏标题：使用 {{ system_name|default('客户管理系统') }}

  4. templates/login.html
  - 页面标题（<title>）：使用 {{ system_name }}
  - 登录框主标题：使用 {{ system_name }}
  - JavaScript品牌更新逻辑（4处）：使用 {{ system_name }}

  核心逻辑

  1. 系统配置页面可编辑"系统名称"
  2. 配置保存到 sys_config 表的 system_name 键
  3. 全局上下文注入 system_name 变量供所有模板使用
  4. 登录页面独立读取配置（无上下文处理器）
  5. 默认值统一为"客户管理系统"

  使用方法

  1. 以超级管理员登录
  2. 进入"系统配置"页面
  3. 修改"系统名称"（如：合同管理系统、资产管理系统等）
  4. 保存后全站生效（登录页、顶部导航栏等）
