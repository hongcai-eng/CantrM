数据库对比结果：

  contracts.db (当前使用，128K) - 有问题：
  - tenant_customer 表缺少 system_name 字段
  - customer 表缺少 business_type 字段

  contracts1.db (备份，92K) - 结构完整：
  - 所有字段都符合 models.py 的定义

  报错原因：

  代码在登录时会读取 tenant_customer.system_name 字段显示品牌信息，但当前数据库缺少这个字段，导致SQL查询失败。

  解决方案：

  有两个选择：

  方案1：使用完整的数据库（推荐）
  # 备份当前有问题的数据库
  cp instance/contracts.db instance/contracts_backup.db

  # 使用结构完整的数据库
  cp instance/contracts1.db instance/contracts.db

  方案2：修复当前数据库（添加缺失字段）
  python fix_db.py

  我帮你创建修复脚本：

●Write(fix_db.py)
  ⎿  Error writing file

──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────── Bash command

   cat > fix_db.py << 'EOF'
   #!/usr/bin/env python
   # -*- coding: utf-8 -*-
   """修复数据库缺失字段"""

   import sqlite3
   import sys

   def fix_database(db_path='instance/contracts.db'):
       """添加缺失的字段"""
       print(f"正在修复数据库: {db_path}")
       print("="*60)

       try:
           conn = sqlite3.connect(db_path)
           cursor = conn.cursor()

           # 检查并添加 tenant_customer.system_name
           cursor.execute("PRAGMA table_info(tenant_customer)")
           columns = [col[1] for col in cursor.fetchall()]

   import sqlite3
   import sys

   def fix_database(db_path='instance/contracts.db'):
       """添加缺失的字段"""
       print(f"正在修复数据库: {db_path}")
       print("="*60)

       try:
           conn = sqlite3.connect(db_path)
           cursor = conn.cursor()

           # 检查并添加 tenant_customer.system_name
           cursor.execute("PRAGMA table_info(tenant_customer)")
           columns = [col[1] for col in cursor.fetchall()]
  解决方案：

  有两个选择：

  方案1：使用完整的数据库（推荐）
  # 备份当前有问题的数据库
  cp instance/contracts.db instance/contracts_backup.db

  # 使用结构完整的数据库
  cp instance/contracts1.db instance/contracts.db

  方案2：修复当前数据库（添加缺失字段）
  python fix_db.py

  我帮你创建修复脚本：

●Write(fix_db.py)
  ⎿  Error writing file

  Read 1 file (ctrl+o to expand)

●现在执行修复脚本来修复当前数据库：

●Bash(python fix_db.py)
  ⎿  instance/contracts.db :�ݿ����޸�����
     ============================================================
     �����ֶ�: tenant_customer.system_name
     … +5 lines (ctrl+o to expand)

●完成！现在验证修复结果：

● Bash(python compare_db_simple.py)                                                                                      ⎿  ======================================================================
     Database Structure Comparison
     ======================================================================
     … +47 lines (ctrl+o to expand)

●✓ 问题已解决

  问题根源：

  你同事替换的数据库文件缺少两个关键字段：
  - tenant_customer.system_name （系统品牌名称）
  - customer.business_type （业务类型）

  登录时系统会读取这些字段来显示品牌信息和客户分类，字段不存在就会报错。

  已完成的修复：

  ✓ 已为 tenant_customer 表添加 system_name 字段
  ✓ 已为 customer 表添加 business_type 字段
  ✓ 数据库结构现在与代码期望完全一致

  现在可以：

  1. 重启应用 start.bat 或 python app.py
  2. 刷新登录页面，应该不会再报错

  预防建议：

  今后如果需要替换数据库，建议使用项目提供的数据库升级脚本：
  python upgrade_db.py

  这个脚本会自动检测并添加缺失的字段，避免因版本不匹配导致的报错。